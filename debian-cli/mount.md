Для временного монтирования
```bash
sudo mount /dev/устройство точка_монтирования
```

Для размонтирования используется один из вариантов команды umount
```bash
sudo umount /dev/устройство
sudo umount точка_монтирования
```
