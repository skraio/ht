Test integrity of zip file archive
```bash
unzip -tq {file.zip}
```

List files and directories
```bash
unzip -l {file.zip}
```

Unzip to directory
```bash
unzip -d /dest/directory/ {file.zip}
```

Multiple unzip
```bash
unzip '*.zip'
```
